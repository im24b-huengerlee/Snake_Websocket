# Snake WebSocket

A minimal Spring Boot + WebSocket snake-style movement demo.

## Run

```powershell
.\mvnw.cmd spring-boot:run
```

Open `http://localhost:8080/`, click **Connect**, then use arrow keys.
